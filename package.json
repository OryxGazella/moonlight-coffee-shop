{
  "name": "moonlight-coffee-shop",
  "version": "1.0.0",
  "description": "Coffee shop point of sale for JavaScript mobbing",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start -s -f index.html src --no-ui --no-notify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10"
  }
}
